# Error Handling Standards

> **This file is a pointer.** The comprehensive error handling reference lives in:

**`.claude/rules/error-handling.md`**

That file contains the full ERROR_CATALOG with 25+ error codes, three-tier classification
(TRANSIENT, RECOVERABLE, FATAL), retry protocols, and standardized `handleError()` /
`withRetry()` functions used across all Agent OS agents.

For project-level application error handling patterns, also see the rules file above.
